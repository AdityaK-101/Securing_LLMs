# Root-level runtime overhead files

`runtime_overhead.csv` / `.json` / `.txt` here mirror the **last**
`python -m src.run_experiments --latency-only` (or a full suite that did not
use `--skip-latency`).

Prefer suite copies for paper numbers:

- `results/suite_a/runtime_overhead.csv`
- `results/suite_b/runtime_overhead.csv`
