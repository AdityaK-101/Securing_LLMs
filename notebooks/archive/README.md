# Archived notebooks

These notebooks are **not** part of the primary evaluation path
(`python -m src.run_experiments`). Kept for historical / exploratory context:

- `experiments.ipynb` — early experiment scratchpad (superseded by CLI)
- `Dataset_metrics.ipynb` — redundant with `notebooks/EDA.ipynb` + `results/figures/eda/`

Provenance notebooks that remain under `notebooks/`:

- `preprocess_*.ipynb`, `combined_create.ipynb`, `EDA.ipynb`
- `data/splits/split.ipynb`
