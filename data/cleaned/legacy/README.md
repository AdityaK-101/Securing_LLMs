# Legacy dataset snapshot (v1.0)

`Securing_LLMs_Combined_Dataset_v1.0_250.csv` is the **deprecated** 250-prompt
corpus (150 injection / 100 benign).

**Do not use for evaluation.** The canonical corpus is:

- `data/cleaned/combined_dataset.csv` (350 prompts)
- `data/splits/{train,val,test}.csv`

See `data_cards/Data_Card.md`.
